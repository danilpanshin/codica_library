h1[align="center"] Categories menu
table.table-hover[align="center"]
  thead
    tr[align="center"]
      th Title:
      th Books:
      th[colspan="5"]
        | Actions:
  tbody      
    = render @categories
-if can? :create, Category
  p= link_to 'New category', new_category_path, class: 'btn btn-success'